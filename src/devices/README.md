# Device parsers

The authoritative device list is `src/devices/registry.py`. Every registered parser implements `BaseDeviceParser` and returns both its native representation and a `NormalizedConfig` snapshot with explicit knowledge state.

## Parser maturity

| Device ID | Parser | Current parser scope |
|---|---|---|
| `IOS_SWITCH`, `IOS_ROUTER`, `IOS_CATALYST` | `CiscoIOSParser` | Verified IOS-family parsing for HTTP/SSH and baseline controls, including effective typed management lines, overlapping-VTY AAA authorization/accounting bindings and explicit named-group membership, SNMPv3 user/group/view/ACL relationships, per-association NTP key/trust state, BGP peer-group/AF/VRF routing trust with bounded direct IPv4 prefix-list effects, explicit-interface OSPFv2 and bounded classic RIPv2/EIGRP authentication, effective CDP/LLDP interface directions, assessed access-edge 802.1X bypass and BPDU-guard inheritance/override state, explicit SSH-suite/key metadata, and secret-free credential formats |
| `IOS_XE` | `CiscoIOSXEParser` | Verified IOS composition, including management-line/AAA/SSH state and typed access-edge VLAN/snooping/DAI/source-guard/port-security, explicit 802.1X bypass and BPDU-guard relationships, plus explicitly scoped or configured IOS-XE MACsec and active crypto references |
| `ASA`, `PIX` | `CiscoASAParser` | Verified typed ASA management services, protocol-specific AAA bindings, explicit local lockout/password-minimum and active SSH/ASDM idle policy, session/SSH state, effective management SSL trustpoint-to-identity-chain attachment and public X.509 assessment, per-server NTP key/trust/algorithm state, ACL, logging, secret-free credentials, and SNMPv3 user/group/host-scope relationships; PIX shares parsing infrastructure but remains unqualified for current ASA 9.x defaults |
| `FORTIOS` | `FortiOSParser` | Verified grammar-aware CLI parsing with VDOM/global scope, mutation ordering, secret-redacted evidence, typed administrator-to-custom-profile write-permission bindings, FortiOS 7.4+ typed RADIUS transport/profile-to-admin bindings, active UTM policy-to-group/member resolution with ordered explicit IPS severity/action filters, per-sink explicit syslog delivery/TLS state including active VDOM overrides, distinct IPv4/IPv6 local-in records, and same-VDOM active IPsec reference chains |
| `JUNOS` | `JunOSParser` | Verified hierarchical and display-set parsing, including effective services, credential formats, named-user login-class/global idle-timeout precedence, pre/post-login notices, RADIUS/TACACS+ accounting resolution, explicit J-Web session controls, distinct stateless filters and SRX zone-pair policies, static address/application resolution, attached/interface-bound VPN reference chains, logging, crypto, BGP group/key-chain/AF/routing-instance trust, OSPFv2 interface authentication, and LLDP all-interface inheritance/disablement |
| `SCREENOS` | `JuniperScreenOSParser` | Verified ordered set/unset parsing for management, users and conservative credential/default metadata, objects, policies, logging, and crypto; ScreenOS 6.3 adds typed unmatched-policy state and independent active console/WebUI/auth-server timeout relationships, including policy-context bindings |
| `CHECKPOINT_FW1` | `CheckPointFW1Parser` | Verified S-expression parsing with typed ordered layers, rules, network/service objects, nested groups, negation, VPN/install/track/time context, legacy anonymous-name support, and bounded static address/protocol/port semantic resolution |
| `PAN_OS` | `PaloAltoPANOSParser` | PAN-OS XML device/vsys interfaces, attached management profiles, dedicated MGT state, ordered rules and explicit interzone-default overrides, same-vsys/shared security profile groups/members, action state and ordered broad anti-spyware/vulnerability severity selectors, log references, typed administrator role/authentication-profile/MFA evidence, explicit banner/session/lockout settings, attached PAN-OS 10+ management SSH profiles, password policy, DNS, typed primary/secondary NTP authentication, per-user SNMPv3 state, management SSL/TLS certificate assessment, content schedules, system-log forwarding, version, and explicit unknown Panorama inheritance |
| `HP_PROCURVE` | `HPProCurveParser` | ArubaOS-Switch running configuration with ordered services, manager/operator credential state, per-channel login/enable methods, remote CLI/serial/WebAgent idle timeouts, MOTD, independent plaintext/TLS WebAgent state, per-user SNMPv3 algorithms/key state and communities, SSH suites, authorized managers, AAA/accounting, password control, VLAN/DHCP-snooping, per-port VLAN/trust/ARP/source-lockdown/port-security relationships, effective syslog destination/Event Log forwarding and explicit remote severity, typed SNTP key/trust associations, version, and model; general service defaults remain limited to 16.10 and administrative/Layer-2/logging-severity records to verified 16.10/16.11 exports |
| `SONICOS` | `SonicOSParser` | SonicOS 7 E-CLI `show current-config custom` only; typed interfaces, management, access rules, VPN proposals, per-user SNMPv3 algorithms/key state, syslog, authenticated NTP, and explicit security-service/dependency state; incompatible formats fail explicitly |
| `ARISTA_EOS` | `AristaEOSParser` | EOS metadata plus active eAPI HTTP/HTTPS, shutdown, VRF/service-ACL and attached SSL-profile scope; effective local users, credential formats and role references; ordered login/EXEC/all-command AAA and accounting; release-aware console/SSH/Telnet session state; distinct login/MOTD banners; explicit global password-minimum policy with named-profile state left unknown; explicit management SSH policy; SNMPv3 user/group/view/VRF-ACL relationships; ordered syslog destinations and explicit trap/buffer severity; and per-VRF symmetric/NTS time associations |
| `F5_BIGIP` | `F5BIGIPParser` | Basic BIG-IP TMOS saved tmsh/SCF object-block parsing for selected SSH, HTTP configuration utility, console, tmsh, password-policy and standard syslog settings, plus explicit enabled virtual-to-Client-SSL bindings and `allow-non-ssl` state. Values are reduced to secret-free typed records; unrelated objects are ignored and malformed/unrelated inputs fail with parse-error coverage. Version is known only from an exported `#TMSH-VERSION` marker. Broader LTM traffic configuration and defaults are not normalized. |

Registered aliases are resolved centrally. Additions must include registry, parser-contract, analyzer-dispatch, and alias tests.

The permanent corpus includes 35 inputs across twelve public pipelines. Its paired and edge-case configurations cover unknown defaults, inactive APIs and objects, format rejection, scope isolation, reference resolution, ordered negation, and bounded administrative-policy state. F5 has paired and syntax-variant basic-support cases.

See [`docs/ARCHITECTURE.md`](../../docs/ARCHITECTURE.md) for parser responsibilities and [`docs/EXTENDING.md`](../../docs/EXTENDING.md) before adding a device or input dialect.
